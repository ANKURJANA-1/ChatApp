<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chatapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/common_google_signin_btn_icon_light_normal"
        android:label="@string/app_name"
        android:roundIcon="@drawable/common_google_signin_btn_icon_light_normal"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChatApp">
        <activity android:name=".allActivity.MessageActivity"></activity>
        <activity android:name=".allActivity.DashboardActivity"></activity>
        <activity android:name=".allActivity.SignupActivity" />
        <activity android:name=".allActivity.LoginActivity">

        </activity>
        <activity android:name=".allActivity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>